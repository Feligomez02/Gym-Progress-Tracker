# ⚠️ CONFIGURACIÓN DE SEGURIDAD ⚠️
# CAMBIA TODOS ESTOS VALORES EN PRODUCCIÓN

# JWT Configuration
SECRET_KEY=tu-clave-secreta-super-segura-aqui-cambiar-en-produccion
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# Database
DATABASE_URL=sqlite:///./gym_tracker.db
# Para producción usa PostgreSQL:
# DATABASE_URL=postgresql://username:password@localhost/gym_tracker

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,https://tu-dominio.com

# Rate Limiting
MAX_REQUESTS_PER_MINUTE=100

# Environment
ENVIRONMENT=development

# 🔐 Para generar una SECRET_KEY segura:
# python -c "import secrets; print(secrets.token_urlsafe(32))"

# 📝 NOTAS DE SEGURIDAD:
# 1. Cambia SECRET_KEY por una clave única y segura
# 2. En producción, usa PostgreSQL en lugar de SQLite
# 3. Configura CORS solo para dominios específicos
# 4. Usa HTTPS en producción
# 5. Implementa logging de seguridad
# 6. Considera usar variables de entorno del sistema
